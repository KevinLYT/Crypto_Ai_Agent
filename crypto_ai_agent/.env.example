# 复制为 .env 后填写。未设置 OPENAI_API_KEY 时：POST /analyze-wallet 走规则模板；POST /agent/analyze-wallet 会返回 503（LangChain 工具调用需要 Key）。
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
